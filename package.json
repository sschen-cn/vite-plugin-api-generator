{
  "name": "vite-plugin-api-generator",
  "version": "0.1.0",
  "description": "api module generator for vite",
  "main": "dist/main.js",
  "types": "dist/main.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rimraf dist/ && tsup src/main.ts --dts --no-splitting"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/node": "^22.5.0",
    "cross-env": "^7.0.3",
    "rimraf": "^6.0.1",
    "typescript": "^5.5.4",
    "vite": "^5.4.2"
  },
  "devDependencies": {
    "tsup": "^8.2.4"
  }
}
